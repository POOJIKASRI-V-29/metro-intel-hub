# ── KMRL Document Intelligence — Python backend dependencies ──
# Pins are lower bounds; adjust to your environment as needed.

# --- Web / API framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9          # required by FastAPI for file uploads
pydantic>=2.6
pydantic-settings>=2.2
typing-extensions>=4.10

# --- LLM / generation ---
openai>=1.30                     # OpenAI-compatible client (also targets local Ollama)

# --- Embeddings / ML ---
sentence-transformers>=2.6
torch>=2.2
numpy>=1.26
tiktoken>=0.6                    # token-aware chunking

# --- Vector stores ---
qdrant-client>=1.9
chromadb>=0.5                    # alternative store used by ChromaVectorStore

# --- Document ingestion / parsing ---
PyMuPDF>=1.24                    # 'fitz' — PDF text extraction
python-docx>=1.1                 # .docx parsing
openpyxl>=3.1                    # .xlsx parsing
Pillow>=10.2                     # image handling for OCR

# --- OCR (optional; also need the native engines installed) ---
pytesseract>=0.3.10             # requires the Tesseract binary on PATH
paddleocr>=2.7                  # requires paddlepaddle; install per your platform
# paddlepaddle>=2.6             # uncomment and pick the right wheel for your OS/GPU
